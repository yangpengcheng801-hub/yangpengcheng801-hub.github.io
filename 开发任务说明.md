# 大学英语四级词汇 App — 开发任务说明

> 项目路径：`cet4-app`  
> 技术栈：React 18 + TypeScript + Vite + Tailwind CSS v3  
> 词库来源：XLS 同步至 `public/words.json`（约 4400+ 词条）

---

## 一、项目目标

在四级词汇资料基础上，做一个**轻量、可本地运行**的背单词 Web 应用，包含：

- **学习**：四选一答题，掌握常考义  
- **复习**：已掌握单词分组造句  
- **错词**：错题本单词分组造句  
- **AI 例句**：对接阿里云百炼（OpenAI 兼容接口），自动换模型、缓存、语法校验  

---

## 二、功能模块

### 2.1 学习页

| 能力 | 说明 |
|------|------|
| 词库加载 | 从 `/words.json` 读取，非硬编码 |
| 学习进度 | `localStorage` 保存当前学习下标 |
| 四选一 | 固定 **4 个选项**：1 正确 + 3 干扰项 |
| 正确选项 | 合并该词**常考义**（规则见下） |
| 干扰项 | 其他单词的常考义合并文案，避免重复 |
| 答错流程 | 错项标红且不可再点；**3 个错项都点过后**才对正确项绿色呼吸提示 |
| 答对后 | 展示「全部释义」：常考义绿色，非常考义默认色 |
| 发音 | 浏览器 `speechSynthesis` |
| 动画 | 切题滑动、选项入场、正确弹跳等（无开关，始终开启） |

**答题页 UI 精简（按需求调整）：**

- 单词与选项之间**不显示**单独词性标签（如 `vt.`）  
- **不显示**「4 选 1，选择正确释义」提示  

**常考义 / 答题选项规则：**

- 多词性（如 `spit`）：每词性取 1 个常考义，用 `；` 连接（`vi.` 与 `vt.` 并存时**去掉 vt.**）  
- 单词性：每词性最多 2 个常考义，用 `，` 连接  
- 偏僻义过滤：人名、地名、过长释义、专业括号标签等不作为常考  

**错词本逻辑：**

- 本题内答错过，即使随后答对也**不**从错词本移除  
- 仅当**本轮未答错且答对**时才从错词本清除  

---

### 2.2 复习页 / 错词页

| 能力 | 说明 |
|------|------|
| 分组 | 每 **10 个词** 一组造一句 |
| 显示模式 | 仅英文 / 仅中文 / 中英对照 |
| 高亮 | 句中目标词可点击，弹出释义弹窗并发音 |
| 刷新 | 单句刷新、全部刷新（带淡出动画） |
| 缓存 | 例句缓存 7 天；规则版本升级后旧缓存失效 |

**造句规则（最终版）：**

1. 一句最多包含该组 **10 个已学/错词**  
2. **每个英文单词在句中只出现 1 次**（允许合理词形变化）  
3. 每次生成/刷新只为该词选用 **一个** 常考义（可换词性、换义项）  
4. 刷新时在多个常考义之间**轮换**（记录在 `senseIndices`）  
5. ~~曾要求一词多义同句多次出现~~ → 已改为**每词一次**  

**语法与质量：**

- 系统提示词强调：**主谓完整、时态一致、自然英语**，禁止逗号堆词、无动词词堆、中式英语  
- 生成温度约 **0.65**（刷新略高），减少胡编  
- 返回后做**自然度校验**（含动词、长度、排除模板堆词句）；不合格自动换模型重试  
- 本地兜底改为 2～3 句有主谓的短句，避免 `word1, word2, word3...` 式乱句  

---

### 2.3 AI 对接（阿里云百炼）

| 能力 | 说明 |
|------|------|
| 接口 | OpenAI 兼容：`/v1/chat/completions` |
| 开发代理 | `scripts/openai-proxy-plugin.ts` → `POST /api/openai/v1/chat/completions` |
| 默认 Base URL | `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| Key | `.env.local` 或页面 AI 设置（仅存浏览器 `localStorage`） |
| 模型列表 | `src/bailianModels.ts` 内置 **80+** 个兼容模型 ID |
| 自动轮换 | 当前模型不可用/额度用尽/限流 → 换下一个；近期失败的模型 **30 分钟内跳过** |
| 记忆 | 上次成功模型写入 `cet4_ai_model_v1`，下次优先使用 |
| 性能 | 单模型超时约 5.5s；单次最多换 **8** 个模型；总耗时上限约 **22s**；同时最多 **2 组** 并发请求 |

**环境变量示例（`.env.local`）：**

```env
OPENAI_API_KEY=sk-你的百炼密钥
VITE_OPENAI_API_KEY=sk-你的百炼密钥
VITE_OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
VITE_AI_MODEL=qwen-plus
```

**启动：**

```bash
cd cet4-app
npm install
npm run dev
```

---

## 三、本地存储 Key

| Key | 用途 |
|-----|------|
| `cet4_mastered_v1` | 已掌握单词及时间 |
| `cet4_wrong_v1` | 错词及错误次数 |
| `cet4_display_mode_v1` | 复习/错词显示模式 |
| `cet4_sentence_cache_v1` | AI/本地例句缓存 |
| `cet4_learn_index_v1` | 学习进度下标 |
| `cet4_openai_key_v1` | 用户填写的 API Key |
| `cet4_ai_model_v1` | 上次成功的百炼模型名 |
| `cet4_ai_failed_models_v1` | 近期失败模型（sessionStorage） |

**例句缓存字段：**

- `en` / `zh`：中英文  
- `wordIds`：本组单词 ID  
- `source`：`ai` \| `local`  
- `promptV`：提示词规则版本（当前 **4**）  
- `senseIndices`：各词本句采用的常考义下标  
- `aiModel`：成功时使用的模型名  

---

## 四、核心代码文件

| 文件 | 职责 |
|------|------|
| `src/App.tsx` | 主应用：学习/复习/错词、释义解析、答题、例句面板、AI 设置 |
| `src/App.css` | 动画与组件样式 |
| `src/aiChat.ts` | 百炼多模型依次请求、超时与校验回调 |
| `src/bailianModels.ts` | 模型列表、失败缓存、轮换顺序 |
| `src/asyncPool.ts` | 例句生成并发池（限制为 2） |
| `scripts/openai-proxy-plugin.ts` | Vite 开发服务器 API 代理 |
| `scripts/sync-xls.mjs` | 从 XLS 同步生成 `public/words.json` |
| `public/words.json` | 运行时词库 |

**主要函数（`App.tsx`）：**

- `parseMeaningGroups` — 按词性拆分释义  
- `getCommonMeaningItems` / `buildQuizCorrectLabel` — 常考义与正确选项文案  
- `buildOptions` — 四选一选项  
- `pickSentenceSenses` / `buildSentenceUserPrompt` — 例句义项选择与提示词  
- `parseAiSentence` / `validateSentenceCoverage` / `isNaturalEnglishSentence` — 例句校验  
- `generateAiSentence` — AI 生成（含兜底）  
- `MeaningReveal` — 答对后全部释义展示  
- `SentencePanel` — 复习/错词例句列表  

---

## 五、UI / 交互要点

- 顶部 Tab：**学习** | **复习** | **错词**（错词显示数量角标）  
- 复习绿底、错词橙底主题  
- AI 状态点：已连 AI / 本地兜底 / 未知  
- 例句来源提示：AI 显示模型名；失败显示橙色本地兜底说明  
- 全部释义区：去掉「答题考查常考义」类说明文字；可选保留「含 X 项拓展义」标签（可按需再删）  

---

## 六、迭代记录（按对话顺序）

1. 搭建 CET-4 词汇 React 应用框架，三 Tab、Tailwind、动画  
2. 词库改为 `words.json`，XLS 同步脚本  
3. 学习页四选一：常考义合并、错项逻辑、全部释义高亮  
4. 去掉答题页词性行、「4 选 1」提示、考查说明文案  
5. 复习/错词 AI 造句：先「一词多义多次」→ 更正为**每词一次、刷新换义**  
6. 百炼**全模型列表 + 自动切换** + 失败模型短期跳过  
7. 刷新变慢问题：**降超时、限重试次数、限并发、失败缓存**  
8. 造句质量：**语法提示词加强、降温、自然度校验、本地兜底改写**  

---

## 七、已知限制与建议

- **生产环境**：当前 AI 代理仅在 `npm run dev` 时生效；正式部署需自建后端转发或配置 CORS 直连百炼。  
- **一组 10 词一句**：对模型要求高，偶发仍可能不够自然，可单句刷新或换 `VITE_AI_MODEL=qwen-flash` 等。  
- **严格校验**：要求每词仅出现 1 次时，词形变化可能导致计数偏差，已尽量用词干匹配。  
- **`.env.local`** 含密钥，勿提交到公开仓库。  

---

## 八、常用命令

```bash
npm run dev      # 本地开发（含 AI 代理）
npm run build    # 类型检查 + 生产构建
npm run preview  # 预览构建结果
npm run sync:pdf # PDF 同步（若使用 PDF 管线）
# 词库：node scripts/sync-xls.mjs（按项目内脚本说明执行）
```

---

*文档根据当前代码与需求对话整理，若后续改规则请同步更新 `SENTENCE_PROMPT_V` 与本文档。*
